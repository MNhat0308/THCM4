@using THCM4.Models;
@model SanPham
@{
    ViewBag.Title = "XemChiTiet";
    Layout = "~/Views/Layout/ProductLayout.cshtml";
}

    <div class="col-md-9 product-price1">
        <div class="col-md-5 single-top">
            <div class="flexslider">
                <ul class="slides">
                    <li data-thumb="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)">
                        <img src="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)" />
                    </li>
                    <li data-thumb="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)">
                        <img src="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)" />
                    </li>
                    <li data-thumb="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)">
                        <img src="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)" />
                    </li>
                    <li data-thumb="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)">
                        <img src="@Url.Content("~/Content/AnhData/"+Model.HinhAnh)" />
                    </li>
                    
         
                </ul>
            </div>
         
            <script defer src="~/Content/js/jquery.flexslider.js"></script>
            <link rel="stylesheet" href="~/Content/css/flexslider.css" type="text/css" media="screen" />

            <script>

$(window).load(function() {
  $('.flexslider').flexslider({
    animation: "slide",
    controlNav: "thumbnails"
  });
});
            </script>
        </div>
        <div class="col-md-7 single-top-in simpleCart_shelfItem">
            <div class="single-para ">
                <h4>@Model.TenSP</h4>
                <div class="star-on">
                    <ul class="star-footer">
                        <li><a href="#"><i> </i></a></li>
                        <li><a href="#"><i> </i></a></li>
                        <li><a href="#"><i> </i></a></li>
                        <li><a href="#"><i> </i></a></li>
                        <li><a href="#"><i> </i></a></li>
                    </ul>
                    <div class="review">
                        <a href="#"> @Model.LuotXem</a>

                    </div>
                    <div class="clearfix"> </div>
                </div>

                <h5 class="item_price">@Model.DonGia.Value.ToString("#,##") VND</h5>
                <p>
                    @Html.Raw(Model.CauHinh)
                </p>
                    <a href="@Url.Action("ThemGioHang","GioHang",new {@MaSp=Model.MaSP,@Surl=Request.Url.ToString() })" class="add-cart item_add">Đặt Hàng</a>
                
            </div>
        </div>
        <h3>Thông tin sản phẩm</h3>
        @Html.Raw(Model.Mota)
        <div class="clearfix"> </div>

     
     
    </div>

